# Cotletter
co~~ver~~ + **t** + letter

Веб-приложение для генерации сопроводительных писем (cover letters) при поиске работы.

[![CI](https://github.com/USERNAME/coletter/actions/workflows/ci.yml/badge.svg)](https://github.com/USERNAME/coletter/actions/workflows/ci.yml)

> **Примечание:** Замените `USERNAME` в badge на имя вашего GitHub пользователя или организации.

## 🧪 Тестирование

Проект использует Vitest для тестирования. Все тесты запускаются автоматически через GitHub Actions при каждом push и pull request.

### Запуск тестов локально

```bash
# Запуск всех тестов
npm test

# Запуск тестов с UI
npm test:ui

# Запуск тестов с покрытием
npm test:coverage
```

### CI/CD

Проект настроен с GitHub Actions для автоматической проверки:
- ✅ Проверка линтера (ESLint)
- ✅ Запуск всех тестов (Vitest)
- ✅ Проверка типов TypeScript
- ✅ Сборка проекта

Workflow файл: `.github/workflows/ci.yml`

## 🚀 Запуск

### Локальная разработка

Для локальной разработки с поддержкой Vercel Serverless Functions используйте Vercel CLI:

```bash
# Установка зависимостей
npm install

# Установка Vercel CLI (если еще не установлен)
npm i -g vercel

# Запуск dev-сервера с поддержкой Serverless Functions
vercel dev
```

Приложение будет доступно по адресу `http://localhost:3000`

**Важно:** Для работы AI-генерации необходимо настроить переменную окружения `OPENAI_API_KEY`. 

1. Скопируйте файл `.env.example` в `.env.local`:
```bash
cp .env.example .env.local
```

2. Откройте `.env.local` и замените `your_openai_api_key_here` на ваш реальный API ключ OpenAI.

### Обычный dev-сервер (без Serverless Functions)

Если нужно запустить только фронтенд без API:

```bash
npm run dev
```

**Примечание:** API endpoints не будут работать в этом режиме.

### Сборка для продакшена

```bash
npm run build
```

### Деплой на Vercel

```bash
# Установка Vercel CLI (если еще не установлен)
npm i -g vercel

# Деплой
vercel
```

После деплоя добавьте переменную окружения `OPENAI_API_KEY` в настройках проекта Vercel:
1. Перейдите в Settings → Environment Variables
2. Добавьте `OPENAI_API_KEY` со значением вашего API ключа OpenAI

## 📋 Что есть

### Страницы

- **Dashboard** (`/`) — главная страница со списком всех созданных писем
- **Generator** (`/generator`) — страница для генерации нового письма

### Основной функционал

✅ **AI-генерация писем** — создание сопроводительных писем через OpenAI API (gpt-3.5-turbo) с потоковой передачей текста  
✅ **Стриминг в реальном времени** — текст письма появляется постепенно по мере генерации  
✅ **Хранение данных** — все письма сохраняются в `localStorage` и восстанавливаются при перезагрузке  
✅ **Мотивационная система** — баннер с целью создать минимум 5 писем и прогресс-индикатор  
✅ **Копирование в буфер** — быстрое копирование текста писем  
✅ **Удаление писем** — возможность удалять созданные письма  
✅ **Адаптивный дизайн** — корректная работа на мобильных устройствах

### Технологии

- **React 19** + **TypeScript**
- **Vite** — сборщик
- **React Router** — маршрутизация
- **Zustand** — управление состоянием с персистентностью в localStorage
- **CSS Modules** — изоляция стилей
- **AI SDK** — интеграция с OpenAI для генерации писем
  - `@ai-sdk/react` — React хуки для стриминга
  - `ai` — основной SDK для работы с AI
  - `@ai-sdk/openai` — провайдер OpenAI
- **Vercel Serverless Functions** — изоляция API вызовов и безопасное хранение ключей

## 🎯 Как работает

### Архитектура

Приложение использует следующую архитектуру:

1. **Клиент (React)** — интерфейс пользователя с формой для ввода данных
2. **Vercel Serverless Function** (`/api/generate-letter`) — изолированный API endpoint для генерации писем
3. **OpenAI API** — генерация текста писем через модель gpt-3.5-turbo
4. **Стриминг** — потоковая передача текста от сервера к клиенту через `@ai-sdk/react`

### Процесс генерации письма

1. **Создание письма:**
   - Переход на страницу Generator через кнопку "Create New"
   - Заполнение формы: должность, компания, навыки, дополнительные детали
   - Нажатие "Generate Now" — отправка данных на `/api/generate-letter`
   - Serverless Function формирует промпт и отправляет запрос в OpenAI
   - Текст письма стримится обратно на клиент и отображается в реальном времени
   - После завершения генерации письмо автоматически сохраняется в `localStorage`

2. **Просмотр писем:**
   - На Dashboard отображаются все созданные письма в виде карточек
   - Можно скопировать текст письма или удалить его

3. **Мотивация:**
   - Пока создано меньше 5 писем — показывается баннер "Hit your goal"
   - Прогресс отображается в формате "X/5 applications generated"
   - После достижения цели баннер скрывается

4. **Хранение:**
   - Все данные сохраняются в `localStorage` под ключом `cotletter-applications`
   - Данные восстанавливаются автоматически при перезагрузке страницы

### Безопасность

- API ключ OpenAI хранится только на сервере (в Vercel Environment Variables)
- Клиент никогда не имеет прямого доступа к API ключу
- Все запросы к OpenAI проходят через Vercel Serverless Function
- Валидация и санитизация всех входных данных на стороне сервера
- Защита от injection атак через санитизацию данных перед отправкой в промпт

## 📁 Структура проекта

```
coletter/
├── api/
│   └── generate-letter/
│       └── route.ts          # Vercel Serverless Function для генерации писем
├── src/
│   ├── components/           # React компоненты
│   ├── pages/                # Страницы (Dashboard, Generator)
│   ├── stores/               # Zustand store для управления состоянием
│   ├── utils/                # Утилиты
│   ├── types/                # TypeScript типы
│   └── assets/               # Шрифты, иконки
├── vercel.json               # Конфигурация Vercel
└── package.json              # Зависимости проекта
```

## 🔧 Настройка окружения

### Переменные окружения

Для работы AI-генерации необходимо настроить `OPENAI_API_KEY`:

**Локальная разработка:**

1. Скопируйте файл `.env.example` в `.env.local`:
```bash
cp .env.example .env.local
```

2. Откройте `.env.local` и замените `your_openai_api_key_here` на ваш реальный API ключ OpenAI:
```bash
OPENAI_API_KEY=sk-...
```

**Примечание:** Файл `.env.example` содержит шаблон с примерами переменных окружения. Файл `.env.local` добавлен в `.gitignore` и не будет закоммичен в репозиторий.

**Production (Vercel):**
1. Перейдите в Vercel Dashboard → ваш проект → Settings
2. Откройте раздел Environment Variables
3. Добавьте переменную:
   - Name: `OPENAI_API_KEY`
   - Value: ваш API ключ OpenAI
   - Environment: Production, Preview, Development (или выберите нужные)

### Получение API ключа OpenAI

1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com)
2. Перейдите в раздел API Keys
3. Создайте новый API ключ
4. Скопируйте ключ и используйте его в настройках окружения

## 🚢 Деплой на Vercel

1. Установите Vercel CLI:
   ```bash
   npm i -g vercel
   ```

2. Войдите в аккаунт:
   ```bash
   vercel login
   ```

3. Деплой:
   ```bash
   vercel
   ```

4. Настройте переменные окружения в Vercel Dashboard (см. раздел "Настройка окружения")

5. При необходимости обновите деплой:
   ```bash
   vercel --prod
   ```
